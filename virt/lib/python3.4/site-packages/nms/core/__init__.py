""" Module nms.core contains data collector and data models for monitoring.

    General workflow of network monitoring is the following:
        1. Configure parameters which represents minimal collected
           piece of data.
        2. Configure devices with parameters. Assemble certain devices
           in groups (called GeneralDevice).
           Several logical devices - bmc, and os in one physical - server.
        3. Connect to database to store information.
        4. Start collector loop in which collect all parameters from every
           device and raise alerts if needed.

    Main modules are:

    - TODO: write modules descriptions.

"""
