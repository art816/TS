
showTimeInterval = 1000;  // ms

/**
 * Get time on server and set it to top block
 */
showTime = function() {
    $.getJSON('/nms/_get_server_time/', function(serverTime) {
        $('#server-time').text(serverTime.time);
    });
}

$(window.setInterval(showTime, showTimeInterval));
