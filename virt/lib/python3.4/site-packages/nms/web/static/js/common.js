/**
 * File: common.js
 * 
 * Main purpose of this module is containing common procedures for all modules.
 *  - blinkIndicator
 */

/**
 * Blinks with indicator
 */
blinkIndicator = function(id) {
    var indicator = $(id);
    setTimeout(function(){
         indicator.animate({ opacity: 0.1, }, 300);
         indicator.animate({ opacity: 1, }, 300);
    });
}
